var num = parseFloat( window.prompt ("Digite um número:"));

if (num%2 == 0) {
    document.write ("<h3> O número " + num + " é par.");
    }
    else{
        document.write ("<h3> O número " + num + " é ímpar.");
    }

